import React from 'react'

import classes from './FilterElement.css'

const filterElement = (props) => {
  return (
    <div className={classes.FilterElement}>
      <div className={classes.Title}>FORSCHUNGSBEREICHE</div>
      <div className={classes.Body}>v</div>
    </div>
  )
}

export default filterElement
